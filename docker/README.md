# Docker Environment (`docker/`)

Contains Dockerfiles and initialization scripts for local deployment.

- `Dockerfile.web`: Production React build.
- `Dockerfile.go_api`: Production Go build.
- `Dockerfile.python_api`: Production Python build.
- `docker-compose-base.yml`: Infrastructure services (MySQL, Redis, MinIO, Infinity).
