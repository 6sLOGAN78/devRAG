# Deep Document Parser (`deepdoc/`)

This directory contains the Python vision and parsing models that extract text, tables, and images from complex PDFs and documents.

## Structure
- `vision/`: YOLOv8 layout detection and table recognition.
- `chunker/`: Semantic chunking boundaries logic.
- `parsers/`: File-specific parsers (PDF, DOCX, HTML).
